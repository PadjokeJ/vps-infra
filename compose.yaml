services:
  curlsite:
    build: ./curl-padjokej.dev
    hostname: curlsite
    restart: unless-stopped
    ports:
      - 8002:8002

  rythmo:
    build: ./pyrythmo
    hostname: rythmo
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - rythmo_stats:/app/stats
  
  caddy:
    image: caddy:2.11.2-alpine
    hostname: caddy
    restart: unless-stopped
    ports:
      - "443:443"
      - "443:443/udp"
      - "80:80"
    volumes:
      - $PWD/caddy:/etc/caddy
      - $PWD/caddy/index.html:/srv/index.html
      - caddy_data:/data

  website:
    image: caddy:2.11.2-alpine
    hostname: website
    restart: unless-stopped
    ports:
      - 8001:80
    volumes:
      - $PWD/website/:/srv
      - $PWD/website/caddy:/etc/caddy

  detek:
    build: ./roboDetekMouche
    hostname: robotkidetek
    restart: unless-stopped
    environment:
      TELOXIDE_TOKEN: ${TELOXIDE_TOKEN}

volumes:
  caddy_data:
  rythmo_stats:
